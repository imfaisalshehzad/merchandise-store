export interface UserSignUpTypes {
    email: string;
    password: string;
}

export interface UserTypes {
    id: string;
    email: string;
    password: string;
    createdAt: string;
    updatedAt: string;
}